// ==========================================================
//  サイト個別のフラグ設定（★public 公開用＝OFF）
//
//  ★ このファイルは private リポジトリの同名ファイルとは値が異なる。
//     private → public の同期時に上書きコピーしないこと。
// ==========================================================
window.SITE_CONFIG = {
  // WEB予約フォーム(reserve.html)への導線を表示するか。
  // バックエンド運用が整うまで公開サイトでは false（非表示）にしておく。
  webReserveEnabled: false
};
